{
  "name": "tolou-concrete-driver-3d",
  "version": "0.2.0",
  "description": "Tolou Concrete Driver 3D - persistent Windows-ready concrete logistics game",
  "main": "electron/main.js",
  "private": true,
  "scripts": {
    "vendor:sync": "node scripts/sync-vendor.js",
    "electron:dev": "npm run vendor:sync && electron .",
    "start": "npm run electron:dev",
    "test:quality": "node scripts/test-quality-model.js",
    "test:progression": "node scripts/test-progression.js",
    "check": "node --check src/game.js && node --check src/concrete/quality-model.js && node --check src/progression/career-profile.js && node --check src/ui/minimap.js && node --check src/ui/operations-polish.js && node --check src/ui/quality-hud.js && node --check src/ui/progression-hud.js && node --check src/platform/persistence.js && node --check src/persistence/session-state.js && node --check electron/main.js && node --check electron/preload.js && node --check electron/persistence.js && node --check scripts/sync-vendor.js && node --check scripts/test-quality-model.js && node --check scripts/test-progression.js && npm run test:quality && npm run test:progression",
    "pack": "npm run vendor:sync && electron-builder --dir",
    "dist:win": "npm run vendor:sync && electron-builder --win nsis --x64"
  },
  "devDependencies": {
    "babylonjs": "9.26.0",
    "electron": "44.3.0",
    "electron-builder": "26.15.3"
  },
  "build": {
    "appId": "ir.tolou.concretedriver",
    "productName": "Tolou Concrete Driver 3D",
    "asar": true,
    "directories": { "output": "dist" },
    "files": ["index.html","src/**/*","vendor/**/*","electron/**/*","assets/**/*","package.json"],
    "win": {
      "target": [{ "target": "nsis", "arch": ["x64"] }],
      "artifactName": "TolouConcreteDriver-Setup-${version}.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "Tolou Concrete Driver 3D",
      "deleteAppDataOnUninstall": false
    }
  }
}
